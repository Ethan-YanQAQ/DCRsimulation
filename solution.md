1.出于作业要求，仅编写单个main.m脚本，不编写额外的函数脚本。

2.信道方面，依据论文并略微简化，可用的信道总数为160，每个小区10个信道，复用距离为4，因此将27*27个小区分成7*7个4*4的复用小区，在考虑将信道分为固定信道和动态信道两个独立的池子，固定信道为（7*7*4*4*每个小区固定信道数）的五维数组，用0/1表示空闲和占用，动态信道为（7*7*总动态信道数）的三维数组；

3.对于call_reqeust，建立结构体记录其状态，结构体包括Call的生成编号(从1开始)，生成坐标/现有坐标（坐标为米制位置坐标），生成时间，通话时间，运动状态，占用信道对应的数组坐标；

4.主函数部分对单小区固定信道个数为0 1 2 3 4 5 8 10分别进行仿真。仿真按时间进行仿真，每一秒为一次循环，先该循环遍历所有call_requests，更新位置坐标、通话状态，再更新信道占用，接着按照论文给出的概率分布生成新call_requests，（全网共用一个 Poisson 到达过程，均匀随机分配到各小区）按策略分配信道或者阻塞通话，在这个过程中记录要求的信道的各项指标，仅观测中间15*15个小区的数据；

5.功能函数包括信道分配函数、信道重分配等函数。简化RING策略，在分配信道时，根据生成坐标查询信道数组，从小到大地分配固定信道，若对应位置固定信道已满则分配对应动态信道池的信道，分配到动态信道的注意检查隔壁复用小区群组的同序列信道对应的通话是否满足复用距离大于4，若对应动态信道池已满则阻塞。当固定信道的通话结束或者进行了边界切换时，若其本身是序列号最大的固定信道占用call则释放信道无事发生，若对应位置的固定信道未满则将序列号最大的固定信道通话切到使用完毕的固定信道，若对应位置的固定信道已满则将该小区的序列号最小的动态信道切换到该使用完毕的固定信道。
